# EmuCoreV Drivers

This repository hosts the lightweight GPU driver catalog used by EmuCoreV.

`drivers.json` contains metadata and download URLs for Adreno-compatible driver packages. Driver ZIP files are not stored in this repository; entries link to their upstream release assets and include source/credits fields.
